# Installation

Follow `install.sh`

# Download models

Setup download in a custom cache directory by updating path in `src/openpi/shared/download.py` and run `download_data.py`

This downloads the checkpoint to the specified cache path

# Evaluating a checkpoint on Libero

Follow setup in `eval.md`

To do this spin up a server that serves the model for querying and run evaluation in another terminal
